import React from 'react';
import { expect, assert } from 'chai';

import app from '../src/App';

describe('Flag-Picker-App', function () {
    it('print hello world', function () {
        let v = "hello world";
        assert.equal(v, "hello world");
    });
});

